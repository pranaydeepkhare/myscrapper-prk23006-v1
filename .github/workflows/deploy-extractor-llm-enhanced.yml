name: Deploy Enhanced LLM Extractor
# Location: .github/workflows/deploy-extractor-llm-enhanced.yml

on:
  workflow_dispatch:
  push:
    paths:
      - 'cloud_function/extractor-llm-enhanced/**'

permissions:
  id-token: write
  contents: read

env:
  PROJECT_ID: ${{ vars.PROJECT_ID }}
  REGION: ${{ vars.REGION }}
  FUNCTION_NAME: extractor-llm-enhanced
  WORKLOAD_IDENTITY_PROVIDER: ${{ vars.WORKLOAD_IDENTITY_PROVIDER }}
  DEPLOYER_SA: ${{ vars.DEPLOYER_SA }}
  # FIX for Node.js deprecation warning
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA }}

      - name: Deploy Cloud Function
        run: |
          gcloud functions deploy ${{ env.FUNCTION_NAME }} \
            --gen2 \
            --runtime=python311 \
            --region=${{ env.REGION }} \
            --source=cloud_function/extractor-llm-enhanced \
            --entry-point=llm_extract_enhanced_http \
            --trigger-http \
            --allow-unauthenticated \
            --timeout=540s \
            --memory=2Gi \
            --set-env-vars PROJECT_ID=${{ env.PROJECT_ID }},GCS_BUCKET=${{ vars.BUCKET_NAME }},REGION=${{ env.REGION }}
